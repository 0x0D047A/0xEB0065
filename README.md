# 0xEB0065

## Usage

```bash
# Provide Packtpub email & password (see config example at ./assets/config.example.json).
vim ./assets/config.json

# Compile TypeScript.
npm run build

# Run download script.
npm run start -- downloadBooks

# Convert markdown to epub (requires pandoc).
./build-epub.ps1
```